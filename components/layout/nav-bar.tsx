import { Button } from '@nextui-org/react';
import { Logo } from '../icons';

export const Navbar = () => {
  return (
    <>
      <Button>=</Button>
      <Logo height={100} width={200} />
    </>
  );
};
